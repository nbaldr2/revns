#!/usr/bin/expect -f
set timeout 120
spawn ssh -o StrictHostKeyChecking=no root@173.199.93.201 "cd /root/revns/revns && docker-compose -f docker-compose.prod.yml restart api && sleep 5 && docker logs --tail 50 revns-api"
expect "*?assword:*" {
    send {4{yW+.[7*F[#BP}(}
    send "\r"
}
expect eof
